% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channels.R
\name{setElectrodePositions}
\alias{setElectrodePositions}
\title{Set electrode positions}
\usage{
setElectrodePositions(x, positions)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{positions}{A data.frame or matrix with columns x, y, z and rows
matching channels. Row names should match channel names.}
}
\value{
Modified PhysioExperiment object.
}
\description{
Assigns 3D electrode positions to channels.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(300), nrow = 100, ncol = 3)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz")),
  samplingRate = 100
)

# Set electrode positions
positions <- data.frame(
  x = c(0, 0, 0),
  y = c(0.71, 0, -0.71),
  z = c(0.71, 1, 0.71)
)
pe <- setElectrodePositions(pe, positions)
}
