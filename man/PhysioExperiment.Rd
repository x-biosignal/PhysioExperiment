% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PhysioExperiment-class.R
\name{PhysioExperiment}
\alias{PhysioExperiment}
\title{Construct a PhysioExperiment object}
\usage{
PhysioExperiment(
  assays = S4Vectors::SimpleList(),
  rowData = NULL,
  colData = NULL,
  metadata = list(),
  samplingRate = as.numeric(NA)
)
}
\arguments{
\item{assays}{A \code{SimpleList} (or coercible object) of assay arrays.}

\item{rowData}{Feature-level metadata as a \code{DataFrame}.}

\item{colData}{Sample-level metadata as a \code{DataFrame}.}

\item{metadata}{Optional experiment-level metadata list.}

\item{samplingRate}{Numeric scalar sampling rate in Hz.}
}
\value{
A \code{PhysioExperiment} instance.
}
\description{
Construct a PhysioExperiment object
}
\examples{
# Create a simple PhysioExperiment with random EEG-like data
# 1000 time points, 4 channels
eeg_data <- matrix(rnorm(1000 * 4), nrow = 1000, ncol = 4)
colnames(eeg_data) <- c("Fz", "Cz", "Pz", "Oz")

pe <- PhysioExperiment(
  assays = list(raw = eeg_data),
  colData = S4Vectors::DataFrame(
    label = c("Fz", "Cz", "Pz", "Oz"),
    type = rep("EEG", 4)
  ),
  samplingRate = 250
)
pe

# Access sampling rate
samplingRate(pe)

# Create with multiple assays
pe2 <- PhysioExperiment(
  assays = list(raw = eeg_data, filtered = eeg_data * 0.5),
  samplingRate = 500
)
}
