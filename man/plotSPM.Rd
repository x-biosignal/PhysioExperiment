% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-spm.R
\name{plotSPM}
\alias{plotSPM}
\title{Plot SPM result}
\usage{
plotSPM(
  x,
  time_axis = NULL,
  show_threshold = TRUE,
  show_clusters = TRUE,
  title = NULL
)
}
\arguments{
\item{x}{An spm_result object.}

\item{time_axis}{Optional time axis values.}

\item{show_threshold}{Logical; if TRUE, shows significance threshold.}

\item{show_clusters}{Logical; if TRUE, highlights significant clusters.}

\item{title}{Plot title.}
}
\value{
A ggplot object.
}
\description{
Visualizes SPM analysis results with significance thresholds.
}
\examples{
# Create and plot SPM result
set.seed(123)
g1 <- matrix(rnorm(100 * 10), nrow = 100)
g2 <- matrix(rnorm(100 * 10), nrow = 100)
g2[40:60, ] <- g2[40:60, ] + 1.5

pe <- PhysioExperiment(
  assays = list(values = cbind(g1, g2)),
  samplingRate = 100
)
result <- spmTTest(pe, group1 = 1:10, group2 = 11:20)
plotSPM(result)
}
