% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channels.R
\name{channelInfo}
\alias{channelInfo}
\title{Channel information management for PhysioExperiment}
\usage{
channelInfo(x)
}
\arguments{
\item{x}{A PhysioExperiment object.}
}
\value{
A DataFrame with channel information.
}
\description{
Functions for managing channel metadata including labels, types, units,
and electrode positions.
Get channel information
}
\details{
Returns channel metadata as a DataFrame.
Channel information is stored in colData (columns = channels).
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(400), nrow = 100, ncol = 4)),
  colData = S4Vectors::DataFrame(
    label = c("Fz", "Cz", "Pz", "Oz"),
    type = rep("EEG", 4)
  ),
  samplingRate = 100
)

# Get channel information
channelInfo(pe)

# Get channel names
channelNames(pe)

# Get number of channels
nChannels(pe)
}
