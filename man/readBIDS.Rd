% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-bids.R
\name{readBIDS}
\alias{readBIDS}
\title{BIDS Format Support for PhysioExperiment}
\usage{
readBIDS(
  bids_root,
  subject,
  session = NULL,
  task,
  run = NULL,
  modality = c("eeg", "ieeg"),
  load_events = TRUE
)
}
\arguments{
\item{bids_root}{Path to the BIDS dataset root directory.}

\item{subject}{Subject identifier (without 'sub-' prefix).}

\item{session}{Session identifier (without 'ses-' prefix). Optional.}

\item{task}{Task name.}

\item{run}{Run number. Optional.}

\item{modality}{Data modality: "eeg" or "ieeg".}

\item{load_events}{If TRUE, loads events from the events.tsv file.}
}
\value{
A PhysioExperiment object.
}
\description{
Functions for reading and writing data in BIDS (Brain Imaging Data Structure) format.
Supports BIDS-EEG and BIDS-iEEG specifications.
Read PhysioExperiment from BIDS dataset
}
\details{
Reads EEG/iEEG data from a BIDS-compliant directory structure.
}
\examples{
\dontrun{
# Read EEG data from BIDS dataset
pe <- readBIDS("path/to/bids", subject = "01", task = "rest")

# Read with session and run specified
pe <- readBIDS("path/to/bids", subject = "01", session = "01",
               task = "oddball", run = 1, modality = "eeg")

# Read without loading events
pe <- readBIDS("path/to/bids", subject = "02", task = "rest",
               load_events = FALSE)
}
}
