% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channels.R
\name{pickChannels}
\alias{pickChannels}
\title{Pick specific channels}
\usage{
pickChannels(x, channels)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{channels}{Integer indices or character names of channels to keep.}
}
\value{
A new PhysioExperiment with selected channels.
}
\description{
Creates a new PhysioExperiment with only selected channels.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(400), nrow = 100, ncol = 4)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz", "Oz")),
  samplingRate = 100
)

# Pick by index
pe_subset <- pickChannels(pe, c(1, 3))
nChannels(pe_subset)  # 2

# Pick by name
pe_frontal <- pickChannels(pe, c("Fz", "Cz"))
}
