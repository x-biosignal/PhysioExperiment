% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/channels.R
\name{dropChannels}
\alias{dropChannels}
\title{Drop channels}
\usage{
dropChannels(x, channels)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{channels}{Integer indices or character names of channels to drop.}
}
\value{
A new PhysioExperiment without specified channels.
}
\description{
Creates a new PhysioExperiment without specified channels.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(400), nrow = 100, ncol = 4)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz", "Oz")),
  samplingRate = 100
)

# Drop by index
pe_dropped <- dropChannels(pe, 1)
nChannels(pe_dropped)  # 3

# Drop by name
pe_dropped <- dropChannels(pe, c("Fz", "Oz"))
}
