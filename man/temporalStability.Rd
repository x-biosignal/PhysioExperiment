% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-network.R
\name{temporalStability}
\alias{temporalStability}
\title{Compute temporal stability of network}
\usage{
temporalStability(dyn_conn, metric = c("correlation", "distance"))
}
\arguments{
\item{dyn_conn}{Result from slidingWindowConnectivity().}

\item{metric}{Stability metric: "correlation" or "distance".}
}
\value{
A list with stability metrics.
}
\description{
Calculates how stable the network topology is over time.
}
\examples{
set.seed(123)
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(1000 * 4), nrow = 1000, ncol = 4)),
  samplingRate = 100
)
dyn_conn <- slidingWindowConnectivity(pe, window_size = 200, step = 50)
stability <- temporalStability(dyn_conn)
}
