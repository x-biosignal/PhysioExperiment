% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PhysioExperiment-methods.R
\name{cbindPhysio}
\alias{cbindPhysio}
\title{Combine PhysioExperiment objects by channels}
\usage{
cbindPhysio(x, y)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{y}{A PhysioExperiment object to combine.}
}
\value{
Combined PhysioExperiment object.
}
\description{
Combines two PhysioExperiment objects by adding channels.
}
\examples{
pe1 <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(200), nrow = 100, ncol = 2)),
  colData = S4Vectors::DataFrame(label = c("Fz", "Cz")),
  samplingRate = 100
)
pe2 <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(200), nrow = 100, ncol = 2)),
  colData = S4Vectors::DataFrame(label = c("Pz", "Oz")),
  samplingRate = 100
)

# Combine channels
pe_combined <- cbindPhysio(pe1, pe2)
nChannels(pe_combined)  # 4
}
