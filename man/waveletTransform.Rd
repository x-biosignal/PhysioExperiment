% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ops-timefreq.R
\name{waveletTransform}
\alias{waveletTransform}
\title{Wavelet transform}
\usage{
waveletTransform(
  x,
  frequencies = seq(1, 40, by = 1),
  n_cycles = 7,
  channel = 1L,
  sample = 1L
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{frequencies}{Numeric vector of frequencies to analyze.}

\item{n_cycles}{Number of wavelet cycles (can be scalar or vector).}

\item{channel}{Channel index to analyze.}

\item{sample}{Sample index (for 3D data).}
}
\value{
A list containing:
\itemize{
\item \code{power}: Power matrix (frequency x time)
\item \code{phase}: Phase matrix (frequency x time)
\item \code{frequencies}: Frequency vector
\item \code{times}: Time vector
}
}
\description{
Computes the continuous wavelet transform using Morlet wavelets.
}
\examples{
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(500 * 4), nrow = 500)),
  samplingRate = 100
)

# Compute wavelet transform (1-30 Hz)
wt <- waveletTransform(pe, frequencies = seq(1, 30), channel = 1)

# Access power and phase
dim(wt$power)  # frequency x time
}
