% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vis-multichannel.R
\name{plotMultiChannel}
\alias{plotMultiChannel}
\title{Multi-channel visualization functions}
\usage{
plotMultiChannel(
  x,
  channels = NULL,
  sample = 1L,
  style = c("butterfly", "stacked"),
  offset = NULL,
  assay_name = NULL,
  colors = NULL
)
}
\arguments{
\item{x}{A PhysioExperiment object.}

\item{channels}{Integer vector of channel indices to plot. If NULL, plots all.}

\item{sample}{Integer index for the sample (for 3D data).}

\item{style}{Plot style: "butterfly" (overlaid) or "stacked" (offset).}

\item{offset}{Numeric offset between channels for stacked plot.}

\item{assay_name}{Optional assay name. If NULL, uses the default assay.}

\item{colors}{Optional color vector for channels.}
}
\value{
A ggplot object.
}
\description{
Functions for visualizing multiple channels simultaneously.
Plot multiple channels (butterfly or stacked)
}
\details{
Generates a plot showing multiple channels from the signal data.
}
\examples{
# Create example data
pe <- PhysioExperiment(
  assays = list(raw = matrix(rnorm(500 * 4), nrow = 500)),
  rowData = S4Vectors::DataFrame(label = c("Fz", "Cz", "Pz", "Oz")),
  samplingRate = 100
)

# Butterfly plot (all channels overlaid)
plotMultiChannel(pe, style = "butterfly")

# Stacked plot (channels offset vertically)
plotMultiChannel(pe, style = "stacked")

# Plot specific channels
plotMultiChannel(pe, channels = c(1, 3), style = "butterfly")
}
