% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats-tests.R
\name{tTestEpochs}
\alias{tTestEpochs}
\title{Statistical Testing for PhysioExperiment}
\usage{
tTestEpochs(
  x,
  condition1 = NULL,
  condition2 = NULL,
  mu = 0,
  paired = FALSE,
  alternative = c("two.sided", "less", "greater"),
  var.equal = FALSE
)
}
\arguments{
\item{x}{An epoched PhysioExperiment object (4D data).}

\item{condition1}{Indices or logical vector for first condition epochs.}

\item{condition2}{Indices or logical vector for second condition epochs.
If NULL, performs one-sample t-test against mu.}

\item{mu}{Value to test against for one-sample t-test (default: 0).}

\item{paired}{Logical; if TRUE, performs paired t-test.}

\item{alternative}{Alternative hypothesis: "two.sided", "less", or "greater".}

\item{var.equal}{Logical; if TRUE, assumes equal variances.}
}
\value{
A list containing:
\item{t_values}{Matrix of t-statistics (time x channel)}
\item{p_values}{Matrix of p-values (time x channel)}
\item{df}{Degrees of freedom}
\item{n1, n2}{Sample sizes for each condition}
}
\description{
Functions for statistical analysis of physiological signal data,
including t-tests, ANOVA, cluster-based permutation tests, and effect sizes.
Pointwise t-test across epochs
}
\details{
Performs t-tests at each time point and channel, comparing epochs
against a baseline or between two conditions.
}
\examples{
# Create example epoched data
set.seed(123)
epochs <- array(rnorm(100 * 4 * 20 * 1), dim = c(100, 4, 20, 1))
pe <- PhysioExperiment(
  assays = list(epoched = epochs),
  samplingRate = 100
)
# One-sample t-test against zero
result <- tTestEpochs(pe)
# Two-sample t-test comparing conditions
result2 <- tTestEpochs(pe, condition1 = 1:10, condition2 = 11:20)
}
